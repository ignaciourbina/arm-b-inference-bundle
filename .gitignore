# Inference outputs (regenerated by running the collection — never committed)
llm/traces/
outputs/
results/
agora/output/

# Secrets — never commit the real .env (ship .env.example only)
llm/.env
.env

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.numba_cache/

# OS
.DS_Store
